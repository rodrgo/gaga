function max_value = MaxMagnitude(vec)
% max_value = MaxMagnitude(vec) is a duplicate of the function by the same
% name in GAGA.  It returns the largest absolute value of the entries in
% vec.

max_value = max(abs(vec));